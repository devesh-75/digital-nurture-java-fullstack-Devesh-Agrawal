# Exercise 1 & 9: Database & DataSource Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Spring SQL Initialization
spring.sql.init.mode=always
spring.main.banner-mode=off

# Exercise 10: Hibernate-Specific Properties & Performance
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG

# Enable Hibernate Batch Processing for Bulk Operations
spring.jpa.properties.hibernate.jdbc.batch_size=30
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Template Configuration for Secondary DataSource (Exercise 9 Demonstration)
# secondary.datasource.url=jdbc:h2:mem:secondarydb;DB_CLOSE_DELAY=-1
# secondary.datasource.username=sa
# secondary.datasource.password=password
# secondary.datasource.driverClassName=org.h2.Driver
